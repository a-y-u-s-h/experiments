let max_distance;function setup(){createCanvas(windowWidth,windowHeight),noStroke(),max_distance=dist(0,0,width,height)}function draw(){background(map(mouseX,0,width,0,100),map(mouseY,0,height,0,100),map(mouseX+mouseY,0,width+height,0,100)),colorMode(HSL);for(let a=0;a<=width+20;a+=20)for(let c,b=0;b<=height+20;b+=20)c=dist(mouseX,mouseY,a,b),c=66*(c/max_distance),fill(0),ellipse(a,b,c,c)}