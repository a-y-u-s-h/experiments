let data={sketch:{background:"#000000"},terrain:{columns:100,rows:100,scale:10}};function setup(){createCanvas(windowWidth,windowHeight,WEBGL),angleMode(DEGREES),colorMode(HSB,100),data.terrain.rows=round(width/data.terrain.scale+1),data.terrain.columns=round(height/data.terrain.scale+1),fill(255)}function draw(){background(data.sketch.background),orbitControl(),camera(0,0,0),push();for(var a=0.5*-data.terrain.rows,b=0.5*data.terrain.rows;a<b;a+=1){beginShape();for(var c=0.5*-data.terrain.columns,d=0.5*data.terrain.columns;c<d;c+=1)vertex(a*data.terrain.scale,c*data.terrain.scale,1*map(noise(a*sin(0.1*frameCount),c*sin(0.1*frameCount)),0,1,0,100*sin(frameCount)));endShape()}pop(),push();for(var a=0.5*-data.terrain.columns,b=0.5*data.terrain.columns;a<b;a+=1){beginShape();for(var c=0.5*-data.terrain.rows,d=0.5*data.terrain.rows;c<d;c+=1)vertex(c*data.terrain.scale,a*data.terrain.scale,1*map(noise(a*sin(0.1*frameCount),c*sin(0.1*frameCount)),0,1,0,100*sin(frameCount)));endShape()}pop()}